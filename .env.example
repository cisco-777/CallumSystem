# Database connection string (provided by Railway PostgreSQL plugin)
DATABASE_URL=postgresql://user:password@host:5432/database

# Session secret for express-session (generate a random string)
SESSION_SECRET=your-secure-random-session-secret-here

# Node environment
NODE_ENV=production

# Port (automatically provided by Railway, no need to set manually)
# PORT=3000

# Optional: Google Cloud Storage (if using file uploads)
# GCS_BUCKET_NAME=your-bucket-name
# GCS_PROJECT_ID=your-project-id
# GCS_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
